@model IEnumerable<DisasterResponseSystem.Models.InkindTypes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InkindName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QTY)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InkindName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QTY)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a>
               
            </td>
        </tr>
}
    </tbody>
</table>
